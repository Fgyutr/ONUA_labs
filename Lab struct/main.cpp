#include <iostream>
#include <string> 
using namespace std;

void SincAutomat()
{
    string s;
    cout << "Введите строку: "; 
    cin >> s;
    pair<char, bool> point;
    point = make_pair<char, bool>("A", true);
}
